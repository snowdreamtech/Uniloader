---
# =====================================================================
# roles/apps/vars/valkey/native/opensuse.yml
# =====================================================================

valkey_overrides:
  app_packages: ["valkey"]
  app_service: "valkey"
  # WHY: openSUSE uses systemd for service management
  app_init_system: "systemd"
