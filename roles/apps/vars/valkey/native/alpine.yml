---
# =====================================================================
# roles/apps/vars/valkey/native/alpine.yml
# =====================================================================

valkey_overrides:
  app_packages: ["valkey"]
  app_service: "valkey"
  # WHY: Alpine uses OpenRC, service name matches package name
  app_init_system: "openrc"
