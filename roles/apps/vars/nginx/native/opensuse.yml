---
# =====================================================================
# roles/apps/vars/nginx/native/opensuse.yml
# =====================================================================

nginx_overrides:
  app_packages: ["nginx"]
  app_service: "nginx"
  # WHY: openSUSE uses systemd for service management
  app_init_system: "systemd"
