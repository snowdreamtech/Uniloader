---
# =====================================================================
# roles/apps/vars/nginx/native/debian.yml
# =====================================================================

nginx_overrides:
  app_packages: ["nginx"]
  app_service: "nginx"
  # WHY: Debian uses systemd for service management
  app_init_system: "systemd"
