---
# =====================================================================
# roles/apps/vars/redis/native/alpine.yml
# =====================================================================

redis_overrides:
  app_packages: ["redis"]
  app_service: "redis"
  # WHY: Alpine uses OpenRC, service name matches package name
  app_init_system: "openrc"
