---
# =====================================================================
# roles/scenarios/server/vars/redhat.yml
# =====================================================================

server_apps_overrides:
  app_packages:
    - "vim"
