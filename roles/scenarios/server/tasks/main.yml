---
# =====================================================================
# roles/scenarios/server/tasks/main.yml
# =====================================================================

- ansible.builtin.include_role:
  name: native
  tasks_from: os_loader
  vars:
  module_name: "server"
  module_var_prefix: "server"
  module_var_prefixes: ["server_apps"]
