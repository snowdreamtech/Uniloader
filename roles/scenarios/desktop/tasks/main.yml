---
# =====================================================================
# roles/scenarios/desktop/tasks/main.yml
# =====================================================================

- ansible.builtin.include_role:
  name: native
  tasks_from: os_loader
  vars:
  module_name: "desktop"
  module_var_prefix: "desktop"
  module_var_prefixes: ["desktop_apps"]
