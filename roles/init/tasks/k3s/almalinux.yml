---
# =====================================================================
# roles/init/tasks/k3s/almalinux.yml
#
# Purpose:
#   Install K3s on AlmaLinux (delegates to redhat.yml).
# =====================================================================

- ansible.builtin.import_tasks: "redhat.yml"
