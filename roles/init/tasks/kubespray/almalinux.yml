---
# =====================================================================
# roles/init/tasks/kubespray/almalinux.yml
#
# Purpose:
#   Setup Kubespray Controller on AlmaLinux (delegates to redhat.yml).
# =====================================================================

- ansible.builtin.import_tasks: "redhat.yml"
